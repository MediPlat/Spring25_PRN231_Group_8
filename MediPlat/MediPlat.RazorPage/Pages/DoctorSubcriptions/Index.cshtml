@page
@model MediPlat.RazorPage.Pages_DoctorSubcriptions.IndexModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DoctorSubcription[0].EnableSlot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoctorSubcription[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoctorSubcription[0].Doctor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoctorSubcription[0].Subscription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DoctorSubcription) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnableSlot)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subscription.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
